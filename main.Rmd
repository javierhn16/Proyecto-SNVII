---
title: "Modelado de reservas de siniestros IBNR mediante cópulas"
author: 
  - Anthony Mauricio Jiménez Navarro - C24067
  - Gustavo Alberto Amador Fonseca - C20459
  - Javier Antonio Hernández Navarro - C13674
  - Luis Fernando Amey Apuy - C20470
date: "`r Sys.Date()`"
output:
  rmdformats::robobook:
    self_contained: true
    highlight: tango
---

```{r, message=FALSE, echo=FALSE, warning=FALSE}
pacman::p_load(
  tidyverse,
  lubridate,
  ggplot2,
  scales,
  fitdistrplus,
  copula,
  dplyr,
  plotly,
  ChainLadder,
  stats,
  zoo,
  pbapply,
  pracma,
  knitr,
  kableExtra
)
```

```{r, message=FALSE, echo=FALSE, warning=FALSE}
data <- read.csv("data/ausautoBI8999.csv", stringsAsFactors = FALSE) %>%
  arrange(AccMth) %>%
  mutate(
    AccDate = as.Date(AccDate),
    ReportDate = as.Date(ReportDate),
    FinDate = as.Date(FinDate),
    AccMth    = as.yearmon(AccMth, "%Y-%m"),
    ReportMth = as.yearmon(ReportMth, "%Y-%m"),
    FinMth    = as.yearmon(FinMth, "%Y-%m"),
    delay = as.numeric((ReportMth - AccMth)),
    duration = as.numeric(FinMth - AccMth)
  ) %>%
  filter(AccDate > as.Date("1992-12-31")) %>%
  filter(AccDate < as.Date("1999-1-1")) %>%
  filter(ReportDate < as.Date("1999-1-1")) %>%
  filter(!is.na(delay), !is.na(duration), delay >= 0, duration >= 0) %>%
  dplyr::select(-c(4:6, 8:14))
```

# Análisis Exploratorio de Datos

## Distribución del monto del reclamo en escala logarítmica
```{r, message=FALSE, echo=FALSE, warning=FALSE}
fig <- ggplot(data, aes(x = log(AggClaim))) +
  geom_density(
    color = "lightblue",
    linewidth = 1.3,
    adjust = 1.2,
    fill = "lightblue"
  ) +
  geom_histogram(
    aes(y = ..density..),
    fill = "#0073C2FF",
    color = "white",
    bins = 50,
    alpha = 0.6
  ) +
  labs(x = "Monto del reclamo (escala log)", y = "Densidad") +
  theme_minimal()

fig
```

## Número mensual de siniestros reportados
```{r, message=FALSE, echo=FALSE, warning=FALSE}
fig <- data %>%
  mutate(month = floor_date(AccDate, "month")) %>%
  group_by(month) %>%
  summarise(Reclamos = n()) %>%
  ggplot(aes(x = month, y = Reclamos)) +
  geom_line(color = "#E69F00", linewidth = 1) +
  geom_point(color = "#F17F00", size = 1.5) +
  geom_smooth(method = "loess",
              se = FALSE,
              color = "#0072B2") +
  labs(x = "Fecha del accidente (mes)", y = "Número de reclamos") +
  theme_minimal()

fig
```

## Relación entre año de ocurrencia continuo y retraso de reporte
```{r, message=FALSE, echo=FALSE, warning=FALSE}
fig <- ggplot(data, aes(x = log(AggClaim), y = OpTime)) +
  geom_density_2d_filled(contour_var = "density", alpha = 0.85) +
  scale_fill_manual(values = colorRampPalette(c("#02102a", "#3182bd", "#fef0d9", "gray")[4:1])(13)) +
  labs(x = "Monto agregado del reclamo (escala log)", y = "Tiempo operativo (OpTime)", fill = "Densidad") +
  theme_minimal()

fig
```

# Etimación vía Chain-Ladder

## Triángulo de desarrollo
```{r, message=FALSE, echo=TRUE, warning=FALSE}
datos_sli <- data %>%
  mutate(
    ocurrencia = year(AccDate),
    reporte = year(ReportDate),
    k  = pmax(0, reporte - ocurrencia)
  ) %>%
  group_by(ocurrencia, k) %>%
  summarise(inc = sum(AggClaim, na.rm = TRUE), .groups = "drop")

triangulo <- pivot_wider(
  datos_sli,
  names_from = k,
  values_from = inc,
  values_fill = 0
) %>%
  arrange(ocurrencia)
triangulo <- data.frame(triangulo)
rownames(triangulo) <- triangulo[, 1]
triangulo <- triangulo[, -1]
colnames(triangulo) <- 0:max(datos_sli$k, na.rm = TRUE)

cumtria <- function(triangulo) {
  n <- nrow(triangulo)
  mask <- upper.tri(matrix(1, n, n), diag = T)[, n:1]
  acum <- t(apply(triangulo, 1, cumsum)) * mask
  acum[!mask] <- 0
  acum <- data.frame(acum)
  colnames(acum) <- 0:(n - 1)
  return(acum)
}

tria <- cumtria(triangulo)
n <- nrow(tria)
mask <- upper.tri(matrix(TRUE, n, n), diag = TRUE)[, n:1]
tria[!mask] <- NA
tria <- as.triangle(as.matrix(tria))
CL <- MackChainLadder(tria)

(tria <- CL$FullTriangle)
```

## Reserva IBNR usando Chain-Ladder
```{r, message=FALSE, echo=FALSE, warning=FALSE}
sum(tria[,6]-diag(tria))
```

# Estimación vía Cópulas
```{r, message=FALSE, echo=TRUE, warning=FALSE}
empirical_quantile_fun <- function(x) {
  x <- sort(x)
  n <- length(x)
  p <- ((1:n) - 0.5) / n
  function(u) {
    if (any(u <= 0) || any(u >= 1)) {
      u <- pmin(pmax(u, 1e-10), 1 - 1e-10)
    }
    approx(p, x, xout = u, rule = 2)$y
  }
}

# Construcción de variables continuas
prepare_ay_dy <- function(data) {
  if (!inherits(data$AccDate, "Date"))
    data$AccDate <- as.Date(data$AccDate)
  if (!inherits(data$ReportDate, "Date"))
    data$ReportDate <- as.Date(data$ReportDate)
  stopifnot(all(!is.na(data$AccDate)))
  stopifnot(all(!is.na(data$ReportDate)))
  doy_acc <- as.integer(format(data$AccDate, "%j"))
  AY_cont <- as.numeric(format(data$AccDate, "%Y")) + (doy_acc - 1) / 365
  DY_cont <- as.numeric(difftime(data$ReportDate, data$AccDate, units = "days")) / 365
  if (any(DY_cont < 0))
    stop("Hay registros con ReportDate < AccDate")
  data2 <- data
  data2$AY_cont <- AY_cont
  data2$DY_cont <- DY_cont
  data2$AccYear <- as.integer(format(data2$AccDate, "%Y"))
  return(data2)
}

# Ajuste de la cópula
fit_copula_pobs <- function(AY_cont,
                            DY_cont,
                            family = c("frank", "clayton", "gumbel")) {
  family <- match.arg(family)
  n <- length(AY_cont)
  U <- rank(AY_cont, ties.method = "average") / (n + 1)
  V <- rank(DY_cont, ties.method = "average") / (n + 1)
  uv <- cbind(U, V)
  if (family == "frank") {
    cop <- frankCopula(dim = 2)
  } else if (family == "clayton") {
    cop <- claytonCopula(dim = 2)
  } else if (family == "gumbel") {
    cop <- gumbelCopula(dim = 2)
  }
  fit <- fitCopula(cop, data = uv, method = "ml")
  return(list(fit = fit, U = U, V = V))
}

# Probabilidades condicionales de reporte
estimate_conditional_probs <- function(data_prepped,
                                       cop_fit,
                                       simulate_n = 200000,
                                       years = sort(unique(data_prepped$AccYear)),
                                       max_lag = 6,
                                       seed = 12345) {
  set.seed(seed)
  n <- nrow(data_prepped)
  q_AY <- empirical_quantile_fun(data_prepped$AY_cont)
  q_DY <- empirical_quantile_fun(data_prepped$DY_cont)
  fitted_copula <- cop_fit$fit@copula
  uv_sim <- rCopula(simulate_n, fitted_copula)
  Usim <- uv_sim[, 1]
  Vsim <- uv_sim[, 2]
  AY_sim <- q_AY(Usim)
  DY_sim <- q_DY(Vsim)
  occ_year_sim <- floor(AY_sim + 1e-9)
  lag_sim <- floor(DY_sim + 1e-9)
  years_vec <- years
  n_years <- length(years_vec)
  probs_matrix <- matrix(0, nrow = n_years, ncol = max_lag + 1)
  rownames(probs_matrix) <- as.character(years_vec)
  colnames(probs_matrix) <- paste0("lag_", 0:max_lag)
  for (i in seq_len(n_years)) {
    a <- years_vec[i]
    sel <- which(occ_year_sim == a)
    if (length(sel) == 0) {
      probs_matrix[i, ] <- 0
    } else {
      counts <- tabulate(pmin(lag_sim[sel], max_lag) + 1, nbins = max_lag + 1)
      probs_matrix[i, ] <- counts / length(sel)
    }
  }
  return(probs_matrix)
}

# Construcción de los triángulos de desarrollo
build_triangle_from_probs <- function(data_prepped,
                                      probs_matrix,
                                      years = sort(unique(data_prepped$AccYear)),
                                      max_lag = 6) {
  n_years <- length(years)
  triangle <- matrix(0, nrow = n_years, ncol = max_lag + 1)
  rownames(triangle) <- as.character(years)
  colnames(triangle) <- paste0("dev_", 0:max_lag)
  counts <- as.integer(table(factor(data_prepped$AccYear, levels = years)))
  for (i in seq_len(n_years)) {
    triangle[i, ] <- counts[i] * probs_matrix[i, ]
  }
  return(list(triangle = triangle, counts = counts))
}

build_triangle_from_probs_amount <- function(data_prepped,
                                             probs_matrix,
                                             years = sort(unique(data_prepped$AccYear)),
                                             max_lag = 6,
                                             amount_col = "AggClaim") {
  n_years <- length(years)
  triangle <- matrix(0, nrow = n_years, ncol = max_lag + 1)
  rownames(triangle) <- as.character(years)
  colnames(triangle) <- paste0("dev_", 0:max_lag)
  
  sums <- tapply(data_prepped[[amount_col]],
                 factor(data_prepped$AccYear, levels = years),
                 sum)
  
  for (i in seq_len(n_years)) {
    triangle[i, ] <- sums[i] * probs_matrix[i, ]
  }
  
  return(list(triangle_amount = triangle, origin_amount = sums))
}

# Estimación de la reserva IBNR
estimate_copula_runoff <- function(data,
                                   family = c("frank", "clayton", "gumbel"),
                                   simulate_n = 200000,
                                   max_lag = 6,
                                   seed = 12345) {
  family <- match.arg(family)
  data_p <- prepare_ay_dy(data)
  years <- sort(unique(data_p$AccYear))
  fit_obj <- fit_copula_pobs(data_p$AY_cont, data_p$DY_cont, family = family)
  probs <- estimate_conditional_probs(
    data_p,
    fit_obj,
    simulate_n = simulate_n,
    years = years,
    max_lag = max_lag,
    seed = seed
  )
  tri_out <- build_triangle_from_probs(data_p, probs, years = years, max_lag = max_lag)
  tri_amt_out <- build_triangle_from_probs_amount(
    data_p,
    probs,
    years = years,
    max_lag = max_lag,
    amount_col = "AggClaim"
  )
  out <- list(
    params = fit_obj$fit,
    family = family,
    probs = probs,
    triangle_expected_counts = tri_out$triangle,
    triangle_expected_amounts = tri_amt_out$triangle_amount,
    origin_counts = tri_out$counts,
    origin_amounts = tri_amt_out$origin_amount,
    data_prepped = data_p
  )
  return(out)
}

out <- estimate_copula_runoff(data, family = "frank", simulate_n = 200000, max_lag = 5)
```

## Probabilidades condicionales de reporte
```{r, message=FALSE, echo=FALSE, warning=FALSE}
out$probs  
```

## Triángulo de desarrollo
```{r, message=FALSE, echo=FALSE, warning=FALSE}
(final <- t(apply(out$triangle_expected_amounts, 1, cumsum)))
```

## Reserva IBNR usando Cópulas
```{r, message=FALSE, echo=FALSE, warning=FALSE}
sum(final[,6]-diag(final))
```

# Gráficos adicionales 
```{r, message=FALSE, echo=FALSE, warning=FALSE}
col_main   <- "lightblue"
col_line   <- "#0073C2FF"
col_accent <- "#0072B2"
col_orange <- "#E69F00"
col_heat   <- colorRampPalette(c("#02102a", "#3182bd", "#fef0d9", "gray")[4:1])(13)

data_prepped <- out$data_prepped
probs_mat    <- out$probs
years        <- as.integer(rownames(probs_mat))
max_lag      <- ncol(probs_mat) - 1
cop          <- out$params@copula
```

## Densidad conjunta de pseudo-observaciones (U, V)
```{r, message=FALSE, echo=FALSE, warning=FALSE}
AYc <- data_prepped$AY_cont
DYc <- data_prepped$DY_cont
n   <- length(AYc)
U   <- rank(AYc, ties.method = "average") / (n + 1)
V   <- rank(DYc, ties.method = "average") / (n + 1)

uv_df <- data.frame(U = U, V = V)

fig <- ggplot(uv_df, aes(U, V)) +
  stat_density_2d_filled(contour_var = "ndensity", alpha = 0.85) +
  scale_fill_manual(values = col_heat) +
  labs(x = "U = Rank(AY)/(n+1)", y = "V = Rank(DY)/(n+1)") +
  theme_minimal()

fig
```

## Contornos de la cópula ajustada
```{r, message=FALSE, echo=FALSE, warning=FALSE}
uv_grid <- expand.grid(
  u = seq(0.001, 0.999, length.out = 80),
  v = seq(0.001, 0.999, length.out = 80)
)

uv_grid$density <- dCopula(as.matrix(uv_grid), cop)

fig <- ggplot(uv_grid, aes(u, v, z = density)) +
  geom_contour_filled() +
  scale_fill_manual(values = col_heat) +
  labs(x = "U", y = "V") +
  theme_minimal()

fig
```

## Probabilidades condicionales por año de accidente
```{r, message=FALSE, echo=FALSE, warning=FALSE}
prob_df <- as.data.frame(as.table(probs_mat))
colnames(prob_df) <- c("AccYear", "lag_label", "prob")

prob_df$AccYear <- as.integer(as.character(prob_df$AccYear))
prob_df$lag     <- as.integer(sub("lag_", "", prob_df$lag_label))

recent_years <- tail(sort(unique(prob_df$AccYear)), 4)
prob_df_plot <- prob_df |> filter(AccYear %in% recent_years)

fig <- ggplot(prob_df_plot, aes(
  x = lag,
  y = prob,
  color = factor(AccYear)
)) +
  geom_line(linewidth = 1) +
  scale_color_manual(values = c(col_accent, col_orange, col_line, "gray40"),
                     name   = "Año de accidente") +
  scale_x_continuous(breaks = 0:max_lag) +
  labs(x = "Lag de desarrollo (años)", y = "Probabilidad") +
  theme_minimal()

fig
```

## IBNR esperado por año de accidente (aprox) 
```{r, message=FALSE, echo=FALSE, warning=FALSE}
origin_counts <- out$origin_counts
ibnr_frac     <- rowSums(probs_mat[, colnames(probs_mat) != "lag_0", drop = FALSE])
ibnr_counts   <- origin_counts * ibnr_frac

ibnr_df <- data.frame(AccYear = years, IBNR    = ibnr_counts)

fig <- ggplot(ibnr_df, aes(x = AccYear, y = IBNR)) +
  geom_col(fill = col_line, alpha = 0.9) +
  labs(x = "Año de accidente", y = "IBNR esperado (reclamos)") +
  theme_minimal()

fig
```

## Modelo con Cópulas vs. Chain Ladder
```{r, message=FALSE, echo=FALSE, warning=FALSE}
reserva_copula <- sum(final[, ncol(final)] - diag(final))
reserva_chainladder <- sum(tria[, ncol(tria)] - diag(tria))

tabla_comp <- data.frame(
  Metodo = c("Cópulas", "Chain-Ladder"),
  Reserva = c(reserva_copula, reserva_chainladder)
)

col_line   <- "#0073C2FF"
col_accent <- "lightblue"

fig <- ggplot(tabla_comp, aes(x = Metodo, y = Reserva, fill = Metodo)) +
  geom_col(alpha = 0.9) +
  scale_fill_manual(values = c("Cópulas" = col_line, "Chain-Ladder" = col_accent)) +
  labs(y = "Reserva estimada", x = "") +
  theme_minimal(base_size = 13) +
  theme(legend.position = "none")

fig
```

